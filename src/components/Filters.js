import React from 'react';

const Filters = () => {
    return (
        <div>
            <h2>Filter Events</h2>
            {/* Add filter options */}
        </div>
    );
};

export default Filters;
